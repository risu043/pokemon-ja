export type Items = {
  name: string;
  jaName: string;
};

export type Names = {
  name: string;
  jaName: string;
};

export type PokemonNamesAndText = {
  name: string;
  text?: string;
};

export type PokemonPropaties = {
  id: number;
  thumbnail: string;
  image: string;
  name: string;
  text?: string;
  types: string;
  abilities: string;
  weight: number;
  height: number;
};
